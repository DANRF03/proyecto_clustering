# proyecto_clustering
Métodos realizados : K.means, DBSCAN, Agglomerative, Birch

### Clonar el Repositorio
```bash
git clone https://github.com/DANRF03/proyecto_clustering.git
cd proyecto_clustering

### Activar el entorno virtual

# Windows
venv_estudiantes\Scripts\activate

# Linux/Mac
source venv_estudiantes/bin/activate

### Instalar dependencias
pip install -r requirements.txt

### Compilar método correspondiente
# 1. EJECUTAR K-MEANS (ANÁLISIS PRINCIPAL)
python clustering_kmeans_datos_limpios.py

# 2. EJECUTAR DBSCAN 
python DBSCAN.py

# 3. EJECUTAR AGGLOMERATIVE CLUSTERING
python agglomerative_ward.py

# 4. EJECUTAR BIRCH CLUSTERING
python birch_clustering.py
🎯 VERSIÓN SIMPLIFICADA PARA README.md
markdown
## ⚡ Ejecución Rápida

```bash
# Instalar dependencias
pip install -r requirements.txt

# Ejecutar todos los algoritmos de clustering

python clustering_analisis.py           # K-Means
python DBSCAN.py                          # DBSCAN
python agglomerative_ward.py              # Agglomerative
python birch_analisis.py                # BIRCH


